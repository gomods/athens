# Go
# Build your Go project.
# Add steps that test, save build artifacts, deploy, and more:
# https://docs.microsoft.com/azure/devops/pipelines/languages/go

pool:
  vmImage: 'vs2017-win2016'

variables:
  GOBIN:  '$(GOPATH)/bin' # Go binaries path
  GOROOT: '/usr/local/go1.11' # Go installation path
  GOPATH: '$(system.defaultWorkingDirectory)/gopath' # Go workspace path
  modulePath: '$(GOPATH)/src/github.com/$(build.repository.name)' # Path to the module's code

steps:
- powershell: |
    mkdir "$env:GOBIN"
    mkdir "$env:GOPATH\pkg"
    mkdir "$env:modulePath"
    Copy-Item "$env:system_defaultWorkingDirectory" -Destination "$env:modulePath" -Recurse
    Remove-Item "$env:modulePath\gopath" -Force -Recurse
    Write-Host "##vso[task.prependpath]$env:GOBIN"
    Write-Host "##vso[task.prependpath]$env:GOROOT\bin"
  displayName: 'Set up the Go workspace'

- script: |
    go version
  workingDirectory: '$(modulePath)'
  displayName: 'Print version'
